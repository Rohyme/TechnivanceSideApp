<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rohyme.navigation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainContainer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".entranceScreens.HomeScreen">
            <nav-graph android:value="@navigation/navgraph_home" />
            <nav-graph android:value="@navigation/navgraph_friends" />
            <nav-graph android:value="@navigation/navgraph_add_friend" />
            <nav-graph android:value="@navigation/navgraph_star" />
            <nav-graph android:value="@navigation/navgraph_messages" />
        </activity>
    </application>

</manifest>